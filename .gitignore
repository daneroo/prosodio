# volatile local outputs / scratch
data

# test fixtures (recreate via scripts/fetch-and-check-fixtures.ts): ignore only
# the large fetched audiobook .m4b (~98MB). The small produced audio m4b, the
# epub, jfk.mp3, and the manifest are all committed.
fixtures/audiobooks/**/*.m4b

# dependencies (bun install)
node_modules

# dotenv environment variable files
.env
.env.local
# .env.development.local
# .env.test.local
# .env.production.local

# Finder (MacOS) folder config
.DS_Store

# Claude Code local tooling: dev-server launch configs + session permissions.
# Single-developer, machine-local; stays on disk across sessions, not shared.
.claude/
