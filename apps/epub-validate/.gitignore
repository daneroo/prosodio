# Built browser bundle — generated by `bun run build:browser`; never edited,
# so prettier/eslint exclude it too (root .prettierignore, eslint ignores).
dist/

# Parser-comparison reports derived from private corpora: PRIVATE (see
# docs/PRIVACY.md). Contains a nested LOCAL-ONLY git repo (never pushed) for
# regression history without publication — regeneration preserves its .git.
reports/
